# 基础环境准备
## 准备环境
192.168.3.201 zhaohui-1  
192.168.3.202 zhaohui-2  
192.168.3.203 zhaohui-3  

## 关闭swap分区
```
swapoff -a
sed -ri '/^[^#]*swap/s@^@#@' /etc/fstab
```

## 配置Hosts文件
```
cat >> /etc/hosts <<EOF
192.168.3.201 zhaohui-1  
192.168.3.202 zhaohui-2  
192.168.3.203 zhaohui-3  
EOF
```

## 网络访问测试
```
ping baidu.com
ping zhaohui-1
ping zhaohui-2
ping zhaohui-3
```

## 开启IP转发
```
cat <<EOF | tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF

modprobe overlay
modprobe br_netfilter

cat <<EOF | tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF

sysctl --system
```

## 设置时区
```
# 上海时区
ln -svf /usr/share/zoneinfo/Asia/Shanghai /etc/localtime
date -R
```

# 安装Kubernetes组件
## 添加Kubernetes源
在所有节点上添加Kubernetes的阿里云源
```
apt-get update && apt-get install -y apt-transport-https
curl -fsSL https://mirrors.aliyun.com/kubernetes-new/core/stable/v1.28/deb/Release.key |
    gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://mirrors.aliyun.com/kubernetes-new/core/stable/v1.28/deb/ /" |
    tee /etc/apt/sources.list.d/kubernetes.list
apt-get update
```

## 安装kubeadm、kubelet、kubectl
在所有节点上安装指定版本的Kubernetes组件
```
apt install -y kubeadm=1.28.15-1.1 kubelet=1.28.15-1.1 kubectl=1.28.15-1.1
```
安装完成，检查组件版本
```
kubeadm version
kubectl version
kubelet --version
```

## 安装Container Runtime（Containerd）
修改仓库为阿里源
```
cat <<EOF | tee /etc/apt/sources.list
# 将内容清空并更改为以下内容
deb https://mirrors.aliyun.com/ubuntu/ jammy main restricted universe multiverse
deb-src https://mirrors.aliyun.com/ubuntu/ jammy main restricted universe multiverse

deb https://mirrors.aliyun.com/ubuntu/ jammy-security main restricted universe multiverse
deb-src https://mirrors.aliyun.com/ubuntu/ jammy-security main restricted universe multiverse

deb https://mirrors.aliyun.com/ubuntu/ jammy-updates main restricted universe multiverse
deb-src https://mirrors.aliyun.com/ubuntu/ jammy-updates main restricted universe multiverse

# deb https://mirrors.aliyun.com/ubuntu/ jammy-proposed main restricted universe multiverse
# deb-src https://mirrors.aliyun.com/ubuntu/ jammy-proposed main restricted universe multiverse

deb https://mirrors.aliyun.com/ubuntu/ jammy-backports main restricted universe multiverse
deb-src https://mirrors.aliyun.com/ubuntu/ jammy-backports main restricted universe multiverse
EOF

apt update && apt upgrade -y
```

### 卸载原先的docker
```
apt-get autoremove -y docker docker-ce docker-engine  docker.io  containerd runc docker-ce-*

rm -rf /etc/systemd/system/docker.service.d

rm -rf /var/lib/docker
```

### 安装containerd
生成默认的配置
```
apt install -y containerd

# 生成默认配置
mkdir -p /etc/containerd
containerd config default | tee /etc/containerd/config.toml
```

### 配置containerd，使用阿里云镜像

```
# 修改config.toml文件，使用阿里云镜像
sed -i 's#sandbox_image = ".*"#sandbox_image = "registry.aliyuncs.com/google_containers/pause:3.9"#' /etc/containerd/config.toml

# 设置SystemdCgroup为true
sed -i 's#SystemdCgroup = false#SystemdCgroup = true#' /etc/containerd/config.toml
```

### 启动containerd
```
systemctl daemon-reload
systemctl restart containerd.service
systemctl enable --now containerd

systemctl status containerd

# 查看containerd版本
ctr version
```

# 初始化Kubernetes集群（Master）
## 使用 kubeadm 初始化 master 节点
在Master节点上使用kubeadm初始化集群
```
kubeadm init --kubernetes-version=v1.28.15 --image-repository registry.aliyuncs.com/google_containers --pod-network-cidr=10.100.0.0/16  --service-cidr=10.200.0.0/16 --service-dns-domain=cxjyyds.com --apiserver-advertise-address=192.168.3.201
```
## 配置kubeconfig文件
```
mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config
```
## 检查控制平面组件状态
```
kubectl get cs
```

# 加入Worker节点
## 在Worker节点执行加入集群命令
在Worker节点上执行kubeadm join命令，加入集群
```
# 根据Master上kubeadm init生成的命令执行
kubeadm join 192.168.3.201:6443 --token luhq5o.jqafdmuxj6dzshf7 \
	--discovery-token-ca-cert-hash sha256:63a122759db5b72b0d95f054ac293c0faf02938a99035564ca87dff03bb59ef0 
```

## 检查集群节点状态
```
# 检查状态为NotReady，需要部署网络插件
kubectl get nodes
```

# 部署Calico CNI插件
## 下载Calico的资源清单并修改Pod网段
```
# 下载Calico资源清单
kubectl create -f https://raw.githubusercontent.com/projectcalico/calico/v3.28.3/manifests/tigera-operator.yaml

wget https://raw.githubusercontent.com/projectcalico/calico/v3.28.3/manifests/custom-resources.yaml
```

```
# 修改文件中的Pod网段，匹配集群配置
grep 'cidr' custom-resources.yaml
      cidr: 192.168.0.0/16
sed -i '/cidr/s#192.168#10.100#' custom-resources.yaml
grep 'cidr' custom-resources.yaml
      cidr: 10.100.0.0/16
```

## 创建资源
下载镜像需要配置代理，无法配置就国内下载好导入。calico.tar.gz
```
ctr -n k8s.io i import calico.tar.gz
```
应用Calico资源清单
```
kubectl create -f custom-resources.yaml
```

## 检查Pod是否部署成功
```
kubectl get pod -A -owide

# 检查node状态为Ready
kubectl get nodes
```

# 部署Nginx应用测试
## 编写Nginx资源清单
写一个nginx.yaml，定义Nginx的Deployment和Service
```
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    app: nginx
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      containers:
      - name: nginx
        image: nginx:1.27.4
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: nginx-service
spec:
  selector:
    app: nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30001   
  type: NodePort
```
## 导包并创建pod
```
ctr -n k8s.io i import nginx-v1.27.4.tar.gz

kubectl apply -f nginx.yaml
```

# 启动kubectl自动补全功能
```
kubectl completion bash > ~/.kube/completion.bash.inc
echo source '$HOME/.kube/completion.bash.inc' >> ~/.bashrc
source ~/.bashrc
```